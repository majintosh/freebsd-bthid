#include <sys/param.h>
#include <sys/bus.h>
#include <sys/module.h>
#include <sys/kernel.h>
#include <sys/socketvar.h>
#include <sys/mutex.h>
#include <sys/uio.h>
#include <dev/hid/hid.h>
#include <sys/taskqueue.h>

#include <sys/mbuf.h>
#include <dev/evdev/input.h>

#include "bthid.h"
#include "hid_if.h"

struct bthid_softc {
	struct socket*			ctrl;
	struct socket*			intr;
	struct task			intr_task;
	hid_intr_t*			intr_handler;
	void*				intr_ctx;
	hid_size_t			input_length;
	struct hid_device_info		dinfo;
	struct hid_rdesc_info		rdesc;
};

#define TESTING 1

#if TESTING
static void
test_worker(void *context, int pending)
{
	struct socket *s = (struct socket*) context;
	int flag = MSG_DONTWAIT;
	struct uio thing;
	thing.uio_resid = 1000000;
	thing.uio_td = curthread;
	struct mbuf *m = NULL;
	soreceive(s, NULL, &thing, &m, NULL, &flag);
	if (m != NULL) {
		uint8_t* payload = mtod(m, uint8_t *);
		for (int i = 0; i<min(7, m->m_len/sizeof(uint8_t)); i++)
		{
			printf("%02X ", payload[i]);
		}
		printf("\n");
	}
}

static int
test_upcall(struct socket *s, void *arg, int which)
{
	struct task *intr_task = arg;
	taskqueue_enqueue(taskqueue_thread, intr_task);
	return SU_OK;
}

#endif

#define DSENSE

#ifdef SWITCH
static uint8_t switch_rdesc[] = {
	,
};
#endif

#ifdef DSENSE
static uint8_t dsense_rdesc[] = {
	,
};
#endif

static int
bthid_probe(device_t dev)
{
	return (BUS_PROBE_DEFAULT);
}




static int
socket_close(struct socket* sock)
{
	if (sock == NULL)
		return 0;
	SOCK_RECVBUF_LOCK(sock);
	if (sock->so_rcv.sb_upcall != NULL)
		soupcall_clear(sock, SO_RCV);
	SOCK_RECVBUF_UNLOCK(sock);
	// Need to clear taskqueue threads before closing socket
	soclose(sock);
	return 0;
}

static int
bthid_detach(device_t dev)
{
	struct bthid_softc *sc = device_get_softc(dev);
	socket_close(sc->ctrl);
	socket_close(sc->intr);
	return 0;
}

/* This sets the info to match my switch pro controller */
#define SET_SWITCH_DEVINFO(hw_ptr) \
	strlcpy(hw_ptr->name, "Maj Switch Pro Controller", sizeof(hw_ptr->name)); \
	strlcpy(hw_ptr->serial, "Maj Pro ID", sizeof(hw_ptr->serial)); \
	hw_ptr->idBus = BUS_BLUETOOTH; \
	hw_ptr->idVendor = 0x057e; \
	hw_ptr->idProduct = 0x2009; \
	hw_ptr->idVersion = 0x0001; \
	hw_ptr->rdescsize = sizeof(switch_rdesc);

#define SET_DSENSE_DEVINFO(hw_ptr) \
	strlcpy(hw_ptr->name, "Maj Dsense Controller", sizeof(hw_ptr->name)); \
	strlcpy(hw_ptr->serial, "Maj Dsense ID", sizeof(hw_ptr->serial)); \
	hw_ptr->idBus = BUS_BLUETOOTH; \
	hw_ptr->idVendor = 0x054c; \
	hw_ptr->idProduct = 0x0ce6; \
	hw_ptr->idVersion = 0x0100; \
	hw_ptr->rdescsize = sizeof(dsense_rdesc);


static int
bthid_attach(device_t dev)
{
	struct bthid_softc *sc = device_get_softc(dev);
	bzero(sc, sizeof(struct bthid_softc));
	struct bthid_ivars *ivar = device_get_ivars(dev);
	sc->ctrl = ivar->ctrl;
	sc->intr = ivar->intr;
#ifdef SWITCH
	sc->rdesc.data = switch_rdesc;
	SET_SWITCH_DEVINFO((&sc->dinfo));
#endif

#ifdef DSENSE
	sc->rdesc.data = dsense_rdesc;
	SET_DSENSE_DEVINFO((&sc->dinfo));
#endif
#if TESTING
	TASK_INIT(&sc->intr_task, 0, test_worker, sc->intr);
	SOCK_RECVBUF_LOCK(sc->intr);
	soupcall_set(sc->intr, SO_RCV, test_upcall, &sc->intr_task);
	SOCK_RECVBUF_UNLOCK(sc->intr);
#endif
	device_t child = device_add_child(dev, "hidbus", DEVICE_UNIT_ANY);
	if (child == NULL) {
		printf("Couldn't add hidbus device\n");
		return (ENOMEM);
		bthid_detach(dev);
	}
	device_set_ivars(child, &sc->dinfo);
	bus_attach_children(dev);
	return 0;
}

static void
intr_worker(void* context, int pending)
{
	struct bthid_softc *sc = context;
	
	struct uio uio;
	int flag = MSG_DONTWAIT;
	// We set resid to 1m to drain everything in the buffer. There is a worry that the intr worker would then retrieve
	// multiple input packets at a time rather than just 1, but this shouldn't be an issue, since the worker thread runs
	// as soon as a packet is received. Though we could receive another packet before the worker thread runs...
	// The alternative approach is to use the isize of hid_rdsec_info and add the size of the packet header to it. Gonna implement this later
	uio.uio_resid = sc->rdesc.isize + 1;
	uio.uio_td = curthread;
	struct mbuf *m = NULL;
	soreceive(sc->intr, NULL, &uio, &m, NULL, &flag);
	if (m!=NULL) {
		m_adj(m, 1); // Strip the bluetooth header from the packet
		uint8_t *payload = mtod(m, uint8_t *);
		sc->intr_handler(sc->intr_ctx, payload, m->m_len);
	}
	m_freem(m);
}

static int
intr_upcall(struct socket *s, void *arg, int which)
{
	struct task *intr_task = arg;
	taskqueue_enqueue(taskqueue_thread, intr_task);
	return SU_OK;
}

static void 
bthid_intr_setup(device_t dev, device_t child __unused, hid_intr_t intr,
		void *context, struct hid_rdesc_info *rdesc)
{
	struct bthid_softc *sc = device_get_softc(dev);
	sc->intr_handler = intr;
	sc->intr_ctx = context;
	// Should we just copy the value of rdesc into our softc's rdesc?
	sc->rdesc.isize = rdesc->isize;
	TASK_INIT(&sc->intr_task, 0, intr_worker, sc);
}

static int
bthid_intr_start(device_t dev, device_t child __unused)
{
	struct bthid_softc *sc = device_get_softc(dev);
	SOCK_RECVBUF_LOCK(sc->intr);
	soupcall_set(sc->intr, SO_RCV, intr_upcall, &sc->intr_task);
	SOCK_RECVBUF_UNLOCK(sc->intr);

	return (0);
}

static int
bthid_intr_stop(device_t dev, device_t child __unused)
{
	struct bthid_softc *sc = device_get_softc(dev);
	SOCK_RECVBUF_LOCK(sc->intr);
	if (sc->intr->so_rcv.sb_upcall != NULL)
		soupcall_clear(sc->intr, SO_RCV);
	SOCK_RECVBUF_UNLOCK(sc->intr);
	return (0);
}

static int
bthid_get_rdesc(device_t dev, device_t child __unused, void *buf,
		hid_size_t len)
{
	struct bthid_softc *sc = device_get_softc(dev);
	memcpy(buf, sc->rdesc.data, len);
	return (0);
}



static device_method_t bthid_methods[] = {
	DEVMETHOD(device_probe,		bthid_probe),
	DEVMETHOD(device_attach,	bthid_attach),
	DEVMETHOD(device_detach,	bthid_detach),

	DEVMETHOD(hid_intr_setup,	bthid_intr_setup),
	DEVMETHOD(hid_intr_start,	bthid_intr_start),
	DEVMETHOD(hid_intr_stop,	bthid_intr_stop),
	DEVMETHOD(hid_get_rdesc,	bthid_get_rdesc),

	DEVMETHOD_END
};

static driver_t bthid_driver = {
	"bthid",
	bthid_methods,
	sizeof(struct bthid_softc)
};

DRIVER_MODULE(bthid, bthidbus, bthid_driver, NULL, NULL);
MODULE_VERSION(bthid, 1);
extern driver_t hidbus_driver;
DRIVER_MODULE(hidbus, bthid, hidbus_driver, 0, 0);
